{
  "name": "reunion-countdown",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=reunion-countdown"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.18.0"
  }
}
